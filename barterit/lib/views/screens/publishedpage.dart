// PublishedPage.dart
import 'package:flutter/material.dart';

class PublishedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Published Page")),
    );
  }
}